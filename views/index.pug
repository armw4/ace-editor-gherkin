extends layout

block content
  h1= title
  p Welcome to #{title}
  form#gherkin-form
    div#status-bar.hidden
    div#editor(style="height: 500px; width: 800px")
    div#commandline(style="position: absolute; bottom: 10px; height: 20px; width: 900px;")
    input.button(type="submit" value="Save")
